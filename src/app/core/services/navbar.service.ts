import { Injectable } from "@angular/core";
import { delay, of } from "rxjs";
import { navbarConstants } from "../constants/navbar-constants";

export class DynamicNavbar {

    dataMap = navbarConstants;

}